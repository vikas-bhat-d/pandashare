import Image from "next/image";

export default function Home() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      PandShare (Building in progress...)
    </div>
  );
}
